# OpenChord S1

Reserved for the portable jambox: synth, tracks, the original OpenChord dream.

This folder is empty on purpose. S1 is a later product. Do not build it here yet.

The Daisy Seed prototype that explored this idea is archived at [`../archive/s1-daisy/`](../archive/s1-daisy/). That tree is history, not the S1 codebase. S1 starts clean when M1 is real, likely on a compute module rather than a Seed.

When this product exists it should sit next to M1 as:

```
OPENCHORD
    s1
```
